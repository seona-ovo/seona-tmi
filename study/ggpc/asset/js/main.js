const mainSlide = new Swiper('.sc-visual .swiper',{
    navigation:{
        prevEl:'.prev',
        nextEl:'.next'
    },
    pagination:{
        el:'.pagination'
    }
});

const reviewSlide = new Swiper('.sc-review .swiper',{
    navigation:{
        prevEl:'.prev',
        nextEl:'.next'
    },
    pagination:{
        el:'.pagination'
    }
})